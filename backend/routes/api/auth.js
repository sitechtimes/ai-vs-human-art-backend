const express = require("express");
const router = express.Router();
const authControllers = require("../../controllers/authController");
// Put ROUTES HERE!!
// router.post('/routername').authControllers.designatedFunction
module.exports = router;
